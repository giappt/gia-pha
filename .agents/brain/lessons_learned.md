# LESSONS LEARNED (SỔ TAY KINH NGHIỆM DỰ ÁN FAT)

- **Windows PowerShell Execution Policy & Node Tooling:** Khi chạy lệnh `npm` hoặc `npx` trên môi trường PowerShell Windows có execution policy chặn script (`npm.ps1`), bắt buộc sử dụng `npm.cmd` và `npx.cmd` để chạy lệnh thành công và ổn định mà không bị lỗi `PSSecurityException`.
- **Global Workflows & 2-Way Registry Sync:** Khi bổ sung lệnh Global mới (`/g-*`), bắt buộc phải đăng ký 2 chiều vào `AGENT_ARCHITECTURE.md` (§2 danh sách lệnh & §5 bảng quyết định) và cập nhật `g-help.md` để luôn thỏa mãn bài kiểm tra toàn vẹn Bước 3b của `/g-health`.
- **0-Token Context Monitoring & Local Extensions:** Không dùng LLM prompt để tự đo context window (tránh hiệu ứng Observer Effect làm phình token); giải pháp chuẩn native là xây dựng Local Extension VS Code (`~/.antigravity-ide/extensions/`) đọc session log trực tiếp từ disk và hiển thị qua StatusBarItem.
- **Public Extension Decoupling:** Khi đóng gói công cụ hoặc extension phát hành công khai (Marketplace/Open VSX), bắt buộc phải tách rời (decouple) các lệnh nội bộ cá nhân (như `/g-compact`) thành các chỉ dẫn ngữ cảnh phổ quát (ví dụ: "Prompt AI to summarize state and start a new session") để bất kỳ người dùng nào cũng hiểu và áp dụng được.
- **Marketplace Icon Design:** Icon cho các tiện ích developer tooling trên Marketplace cần theo phong cách phẳng (Flat design), tối giản hiện đại (Swiss design style) với biểu tượng đo tiến độ vòng cung (circular progress gauge), màu gradient tinh tế trên nền squircle tối màu để tạo cảm giác chuyên nghiệp, không dùng hiệu ứng 3D neon quá rườm rà.
- **Anti Box-in-Box UI & Fresh Jade Heritage Design:** Trong thiết kế giao diện phả hệ/văn hóa số, tuyệt đối tránh lối mòn "lồng hộp" (box-in-box: thẻ bọc khối icon vuông bọc viên thuốc tag) và các gam màu nâu đất/hổ phách u tối dễ gây cảm giác đình chùa cũ kỹ; thay vào đó, áp dụng phong cách "Modern Vietnamese Heritage": nền trắng sứ với quầng sáng ngọc bích (Jade Emerald `#059669`), đường kẻ hairline siêu mảnh 1px (`border-slate-200/60`), icon nổi tự do và typography Be Vietnam Pro phân cấp rõ ràng để tạo cảm giác trang trọng, thanh thoát và tràn đầy sinh khí.
- **Offline Resilient Dev Auth & Server-First State:** Khi môi trường mạng chặn Google OAuth / external endpoints, không để client-side gọi `supabase.auth.getUser()` vô hạn định (gây treo UI ở trạng thái Loading). Giải pháp chuẩn: (1) Fast-check sự tồn tại của auth cookie trước khi gọi network; (2) Thiết lập Server Component (`Navbar.tsx`) lấy session trên server và truyền `initialUser`/`initialProfile` xuống client để render tức thì 0ms; (3) Dùng link trực tiếp `/api/auth/dev-login?action=login` set cookie dev (`httpOnly: false`) để bypass hoàn toàn OAuth mạng ngoài mà không cần đợi client JS hydration.
- **Thiên văn Âm lịch Hồ Ngọc Đức & Quy đổi `lunarYear`:** Khi chuyển đổi Dương lịch sang Âm lịch Việt Nam UTC+7, phép so sánh `monthStart` với tháng 11 âm lịch `a11` phải gán `lunarYear = year` (nếu `a11 >= monthStart`) và chỉ trừ 1 năm khi `lunarMonth >= 11 && diff < 4` (giai đoạn trước Tết Nguyên Đán). Tránh nhầm lẫn gán `year - 1` ngay từ đầu khiến các ngày đầu năm âm lịch (như Mùng 3 Tết 19/02/2026) bị lùi sai thành năm trước.
- **Kinship Engine - Tách rời Trưởng/Thứ vs Tuổi đời theo Vùng miền:** Phong tục miền Bắc và miền Trung coi trọng thứ bậc nhánh (vai Bác luôn là Anh/Chị dù ít tuổi hơn); ngược lại miền Nam ưu tiên tuổi đời thực tế (`birth_year`/`birth_date`). Khi thiết kế hàm xưng hô họ hàng, hàm so sánh cùng thế hệ (`resolveSameGeneration`) bắt buộc phải dùng bộ so sánh tuổi đời thuần túy (`compareAge`) cho miền Nam, tách biệt khỏi `compareSeniority` (vốn ưu tiên nhánh trưởng `is_senior_branch` lên đầu).
- **Lạm dụng `browser_subagent` gây cháy quota:** 4 lần gọi liên tiếp mà chưa verify bằng `curl` trước → 429 quota exhausted. Quy trình đúng: (1) curl API xác nhận response đúng, (2) sửa hết bug client-side, (3) gộp toàn bộ kịch bản vào 1 lần gọi browser duy nhất.
- **Khởi tạo state `'use client'` từ mock data:** Select box trống khi chỉ dùng `useEffect` fetch API vì React render trước khi fetch xong. Giải pháp: import trực tiếp `MOCK_CLAN_MEMBERS` để khởi tạo `useState(INITIAL_MEMBERS)` tức thì, đồng thời fetch API để đồng bộ dữ liệu mới nhất.
- **Zero-Latency In-Memory Kinship Calculation & Middleware Bypass:** Đối với các tính toán đồ thị/toán học thuần túy (như LCA & Xưng hô 3 miền), không phụ thuộc vào network `fetch('/api/kinship')` cho mọi tương tác UI vì Next.js middleware gọi `supabase.auth.getUser()` gây nghẽn 15–35s khi mạng chập chờn. Giải pháp chuẩn: (1) Tính toán in-memory trực tiếp trên client bằng các Pure Functions (`findLowestCommonAncestor`, `resolveKinshipTerms`) đạt tốc độ tức thì (< 1ms); (2) Bổ sung `api/kinship` vào matcher exclusion của `src/middleware.ts`; (3) Thiết lập Live Reactivity tự động tính lại ngay trên `onChange` của dropdown và tab vùng miền, đồng thời auto-clean ô tìm kiếm khi click nút kịch bản mẫu.
- **Xung đột giữa `next dev` và `next build` trên thư mục `.next/`:** Khi tiến trình `npm run dev` đang chạy ngầm, việc thực thi lệnh kiểm thử `npm run build` trong cùng workspace sẽ ghi đè và xóa các chunk phát triển trong `.next/server/`, khiến dev server bị sập với lỗi `Server Error: Cannot find module './xxx.js'`. Quy tắc xử lý: Bất cứ khi nào chạy `next build` để kiểm chứng Tầng 1, bắt buộc phải tự động khởi động lại (restart) dev server để nạp lại bundle dev sạch.
- **Trực Hệ Dọc vs Cây Chữ V & Đường nối vuông góc 90 độ:** Khi vẽ quan hệ gia phả trực quan, cần tách biệt rõ 2 mô hình biểu diễn: (1) Quan hệ trực hệ dọc (Cha-Con, Ông-Cháu) phải vẽ trên 1 trục đứng duy nhất (`#direct-lineage-tree`), tuyệt đối không ép vào cây chữ V 2 nhánh để tránh lặp LCA máy móc và tránh nhãn thừa `(Trực hệ từ LCA)`; (2) Quan hệ họ hàng rẽ nhánh chữ V (`#inverted-v-tree`) nên dùng hệ trục đường nối vuông góc nét liền (`solid orthogonal lines`) đồng bộ theo CSS Grid 2 cột thay vì SVG tọa độ cố định, vừa đảm bảo vuông vắn 90 độ vừa tự động co giãn thẳng hàng 100% theo tâm của thẻ node.
- **Tách bạch Danh xưng Ngữ cảnh & Cài đặt Vùng miền Dòng họ:** Không nên bắt người dùng chọn tay vùng miền tràn lan trên trang tra cứu khi dòng họ đã có quy ước mặc định trong Cài đặt (`clan_settings`); đồng thời kết quả xưng hô cần tách bạch rõ giữa danh xưng lễ nghi trong họ tộc (tôn ti theo chi nhánh) và giao tiếp thường nhật (thân mật theo tuổi tác), kèm hướng dẫn thực hành Xưng - Gọi cụ thể để người dùng biết cách xưng hô chính xác trong đời sống.
- **Chống Lạm Dụng Browser Subagent Cho Tinh Chỉnh UI Nhỏ (Anti-Tool Abuse):** Tuyệt đối không giao các kịch bản kiểm thử quá dài dòng gồm nhiều thao tác cuộn/gõ phím/click pixel liên hoàn cho `browser_subagent` khi chỉ thực hiện các thay đổi UI tĩnh (xóa thẻ JSX, đổi text, ẩn badge có điều kiện). Việc này khiến subagent rơi vào vòng lặp trial-and-error (cuộn, tìm kiếm, click lặp lại tới 90 bước), gây lãng phí tài nguyên và thời gian nghiêm trọng. Quy tắc thực tế: Chỉ cần typecheck và unit test sạch 100%, sau đó mời trực tiếp User UAT bằng mắt khi User đã mở sẵn trình duyệt tại trang đó.
- **Minh Bạch Hóa Từ Điển Xưng Hô & Tùy Biến Gia Tộc 2 Chiều:** Không để các tùy chọn cấu hình văn hóa ở dạng "hộp đen" mù mờ (như chỉ có 3 nút radio vùng miền trừu tượng). Phải minh bạch hóa bằng bảng danh mục chi tiết 16 mối quan hệ cốt lõi (Trực hệ, Cùng thế hệ, Bác/Chú/Cô, Họ hàng lệch đời) kèm tính năng chỉnh sửa trực tiếp 2 chiều (A gọi B / B gọi A). Cấu hình tùy biến được lưu dưới dạng `JSONB` trong `clan_settings.custom_kinship_dictionary` và được lõi Kinship Engine ưu tiên nạp động, vừa giữ vững phong tục vùng miền gốc vừa đáp ứng trọn vẹn tập quán xưng hô riêng biệt của từng dòng họ.
- **Cấu Trúc Thân Tộc Việt Nam Toàn Diện (Nội - Ngoại & Dâu - Rể):** Hệ thống xưng hô gia phả chỉ thực sự chuẩn phong tục khi bao phủ đủ 6 chiều quan hệ: (1) Trực hệ nội/ngoại; (2) Cùng đời ruột, họ & dâu/rể; (3) Bác/Chú/Cô bên nội và phu thê (Thím, Dượng nội); (4) Cậu/Dì bên ngoại và phu thê (Mợ, Dượng ngoại); (5) Dâu/Rể thế hệ con cháu; (6) Bậc họ hàng lệch đời. Khi danh mục mở rộng lên 32+ quan hệ, UI bắt buộc trang bị Filter Chips phân nhóm kết hợp ô tìm kiếm tức thì 0ms để tránh tình trạng cuộn trang dài gây mỏi mắt.
- **Code-First Verification Loop & Loại Bỏ Triệt Để Browser Subagent Cho Thẩm Định UI:** Kiểm chứng phần mềm bắt buộc phải lấy mã nguồn và Test Code tự động (`tests/*.test.ts`) làm chân lý thực nghiệm. Mọi logic, thuật toán toán học (LCA, Can Chi, Tree Layout tọa độ) hoặc API DTO mapping phải được chứng minh bằng log `npm test` PASS 100% trên terminal. Tuyệt đối cấm dùng `browser_subagent` để mò mẫm hay làm thay User việc kiểm thử giao diện; 100% việc nghiệm thu thị giác (màu sắc, layout, canvas pan/zoom, responsive) được trao trọn vẹn cho User (Human UAT) trên màn hình trình duyệt thực tế.
- **Tính Nhất Quán Giữa Hạ Tầng Test & Thượng Nguồn Spec:** Khi yêu cầu mọi tính năng phải có Test Code, bắt buộc phải đồng bộ 3 mắt xích: (1) `package.json` dùng wildcard `tests/*.test.ts` để tự động quét mọi file test mới; (2) Thượng nguồn `/feature-spec` chia rõ Mục 7 thành Automated Test Suite (`tests/`) và Human Visual UAT; (3) Lệnh `/feature-code` bắt buộc viết test song hành và cấm tick `[x]` nếu log test chưa chạy qua file test mới.
- **Kỷ Luật Công Cụ & Ngoại Lệ 'Read-Once':** Quy tắc chống lạm dụng công cụ 'Read-once' ở tầng Global cần gắn kèm ngoại lệ rõ ràng: cho phép đọc lại khi file vừa được chỉnh sửa bằng tool edit hoặc lệnh terminal để xác minh kết quả, ngăn AI hiểu máy móc mà từ chối kiểm tra lại file vừa can thiệp.
- **Hệ Thống Anchor ID `[R-*]` & Di Cư Hệ Gen (Genetic Migration):** Khi đồng bộ Global xuống Local qua `/g-pull`, việc dùng số thứ tự sẽ làm gãy tham chiếu chéo do Local có xen kẽ các luật riêng dự án. Giải pháp chuẩn là gắn Anchor ID bất biến `[R-*]` trên từng đề mục; `/g-pull` merge theo ID thay vì theo số thứ tự, bảo toàn 100% luật dự án và triệt tiêu vĩnh viễn nguy cơ tham chiếu sai mục.
- **Quy ước Hôn nhân nội tộc & Ghost Node (Directional Ghost Node Policy):** Khi hai thành viên nội tộc cưới nhau (ví dụ: Tuấn Chi 1 lấy Mai Chi 2), nếu không quy ước chiều rẽ thì cả hai nhánh đều nhận diện phối ngẫu và sinh Ghost Node gây trùng lặp ($28 + 2 = 30$ thẻ). Dựa trên tập quán truyền thống Việt Nam (người nữ theo họ chồng), thuật toán dàn trang quy ước: Người vợ hiển thị dạng `GhostNode` duy nhất bên cạnh chồng tại nhánh của chồng (trỏ ngược về ID gốc ở Chi 2); còn tại nhánh cha mẹ người vợ, cô ấy xuất hiện như con gái ruột và không kéo chồng sang làm rể. Quy tắc này đảm bảo tổng số node trên Canvas luôn là $N + 1$ thẻ chuẩn xác.
- **TypeScript Interface Extensibility cho `@xyflow/react` v12:** Trong React Flow 12, generic `Node<Record<string, unknown>>` yêu cầu kiểu `data` phải có index signature hoặc kế thừa `Record`. Khi khai báo các DTO Node Data cho Canvas (như `TreeNodeData`, `GhostNodeData`), bắt buộc phải cho `extends Record<string, any>` để các hooks `useNodesState<Node<TreeNodeData>>` và `nodeTypes` hoàn toàn tương thích và compile sạch 0 lỗi TypeScript.
- **Zero Node Overhead qua Virtual Relative Handle & Đường Nối Vuông Góc 90°:** Khi thiết kế cây phả hệ trong `@xyflow/react` cần nhánh con cái xuất phát từ trung điểm khoảng trống giữa 2 vợ chồng, nếu chèn dummy/joint node trung gian vào đồ thị sẽ làm phình to DOM và làm sai lệch số lượng node của dòng họ (gãy Unit Tests `nodes.length`). Giải pháp chuẩn mực là sử dụng **Virtual Relative Handle**: đặt Handle con cái tại đáy thẻ Chồng ở tọa độ tương đối `style={{ left: '210px' }}` (`id="children-joint"` — tương ứng đúng tâm điểm khoảng hở $20\text{px}$ giữa Chồng $200\text{px}$ và Vợ $200\text{px}$). Kết hợp cạnh con cái `type: 'step'` vuông góc $90^\circ$ dứt khoát và cạnh hôn phối `type: 'straight'` nối ngang phẳng phiu giữa 2 hông (`spouse-right` $\rightarrow$ `spouse-left`), giao diện phả đồ đạt chuẩn mực trang trọng, loại bỏ hoàn toàn đường xiên chéo $45^\circ$ mà số lượng node giữ nguyên $100\%$ ($0$ node phát sinh).
- **Invisible Handle Styling & Khung Chữ T Nét Liền Đồng Bộ (`┴`):** React Flow mặc định render các chấm tròn màu xám/đen tại các điểm Handle gây mất mỹ quan phả đồ. Giải pháp triệt để là áp dụng CSS tàng hình toàn diện: `className="!opacity-0 !w-0 !h-0 !border-0 !p-0 pointer-events-none"` cho 100% handles. Đồng thời, đặt handle con cái tại `top: 48px, left: 210px` (trung điểm thanh ngang hông nối vợ chồng) kết hợp đồng bộ màu xanh ngọc bích `#059669` và nét liền (`type: 'straight'`), tạo thành kết cấu chữ T ngược (`┴`) vuông vức $90^\circ$ liền mạch, không còn bất kỳ dấu chấm lem nhem nào trên canvas.
- **Phân Tách Quản Trị Nội Tộc vs Ngoại Tộc & Kiểm Soát Mật Độ Hiển Thị (Clan Density Control):**
  1. *Dâu nội tộc (hôn nhân nội tộc):* Thiết lập liên kết 2 chiều thông minh — thẻ gốc ở nhánh cha mẹ (Chi 2) hiển thị badge trang trọng `💍 Chồng: [Tên Chồng] (Chi 1 - Trưởng)` kèm nút `Xem gia đình ↗` lướt camera sang Chi 1; thẻ Ghost Node ở Chi 1 có nút `Vị trí gốc ↗` lướt về Chi 2; bầy con chỉ nằm ở Chi 1 theo họ cha để bảo toàn chuẩn mực số đinh.
  2. *Con gái lấy chồng ngoại tộc:* Cung cấp nút Toggle trực tiếp trên Toolbar: Chế độ `Dòng nội` (mặc định gọn gàng, thẻ con gái chỉ nạp metadata `externalSpouse` ghi rõ thông tin chồng ngoại, ẩn rể ngoại và con ngoại khỏi canvas) vs Chế độ `Mở rộng họ ngoại` (hiển thị đầy đủ rể ngoại và con ngoại khi có nhu cầu tra cứu chuyên sâu). Giải pháp này giúp phả đồ luôn giữ được bố cục thoáng đãng, thanh thoát mà vẫn toàn vẹn thông tin văn hóa gia tộc.
- **Orthogonal Family Bus Hierarchy & Cơ Chế Gốc Tùy Biến Tự Đổi Vai (Focus Root Perspective Engine):**
  1. *Triệt tiêu đường ziczac và chữ V bằng Custom Bus Edge:* Cạnh `step` mặc định của React Flow tự động bẻ góc ngẫu nhiên cho từng đứa con tạo cảm giác "mỗi chỗ 1 kiểu". Giải pháp dứt khoát là Custom Edge `FamilyBusEdge` với công thức SVG path `M sourceX sourceY V midY H targetX V targetY` với $Y_{bus} = Y_{source} + (Y_{target} - Y_{source}) / 2$. Toàn bộ đàn con chia sẻ chung một trục đứng và một thanh bus ngang phẳng lì song song mặt đất, cắm vuông góc $90^\circ$ dứt khoát xuống đỉnh các con.
  2. *Gốc Tùy Biến (Focus Root) & Đổi vai thuần túy theo đồ thị:* Tuyệt đối không hard-code thuộc tính "Chi" cứng nhắc trong CSDL. Xem theo chi nhánh thực chất là duyệt cây con từ một Tiền nhân làm Gốc (`focusRootId`). Khi duyệt từ Gốc $X$, thuật toán phân định: nếu đi qua nhánh con gái của Gốc, chồng của cô ấy tự động trở thành Con rể (`inlawRole: 'son_in_law'`), con cái của cô ấy tự động trở thành Cháu ngoại (`childRole: 'maternal_grandchild'`), phục hồi đầy đủ con cháu cho chi nhánh ngoại mà vẫn bảo toàn số đinh khi xem toàn tộc.
  3. *Tôn ti thứ tự sinh (`birth_order`) trước tuổi đời (`birth_year`):* Khi tiền nhân thất truyền năm sinh, `birth_order` là chìa khóa duy nhất để xếp anh em lớn $\rightarrow$ bé từ trái sang phải; khi có năm sinh, hệ thống tự động suy diễn `birth_order` và bật cảnh báo tương tác nếu phát hiện mâu thuẫn năm sinh.
- **Tôn ti Sinh tử vs Vai vế & Nhận diện Hôn phối nội tộc Đối Xứng 2 Chiều:**
  1. *Trạng thái sinh tử góc trên bên phải:* Vị trí góc trên bên phải của thẻ `MemberNode` tuyệt đối chỉ dành cho trạng thái sinh tử (`✨ Cụ Tổ` cho Đời 1, `† Đã mất` / `Còn sống` cho Đời $\ge 2$); tuyệt đối không nhồi nhét chữ `Con dâu`/`Con rể` vào đây vì sẽ đè mất thông tin sinh tử của Cụ Bà Thủy Tổ và các dâu/rể.
  2. *Thẻ GhostNode dâu/rể nội tộc viền vàng nét đứt:* Thẻ phản chiếu GhostNode mang viền vàng nét đứt (`border-2 border-dashed border-amber-500 bg-amber-50/85`) là chuẩn mực nhận diện node ảo; ở phía ngược lại (thẻ gốc của người đó trên cây huyết thống), thẻ gốc cũng phải mang nhận diện đối xứng viền/badge hổ phách nét đứt để người xem ở cả 2 chi đều nhận ra ngay mối quan hệ hôn phối nội tộc.
  3. *Quy tắc kép Con Trưởng & Thứ tự sinh:* `birth_order` dàn hàng ngang anh chị em từ trái qua phải; con trai lớn nhất mặc định được suy luận là **Trưởng Nam** (`(Trưởng)`) ngay cả khi sau chị gái, đồng thời hỗ trợ cờ `is_senior` thủ công cho các ca ngoại lệ (anh cả mất sớm).
  4. *Phân tầng Z-Index chống va chạm layout:* Header Navbar `z-50`, User Profile Dropdown `z-[100]`, Canvas Toolbar `z-30`, Dropdown/Search trong Toolbar `z-40` triệt tiêu dứt điểm lỗi các popup/menu đè hoặc cắt ngang nhau.
- **Đối Xứng Hôn Phối Nội Tộc 2 Chiều & Quy Ước "Nam Tả Nữ Hữu" (Bidirectional Internal Marriage & Gender Position):**
  1. *Tính đối xứng 2 chiều hoàn hảo:* Trong hôn nhân nội tộc (ví dụ Tuấn Chi 1 lấy Mai Chi 2), cả 2 nhánh đều có thẻ Ghost Node phản chiếu của đối tác: Chi 1 có Ghost Node của vợ (Mai - Dâu nội tộc, đặt bên phải chồng); Chi 2 có Ghost Node của chồng (Tuấn - Rể nội tộc, đặt bên trái vợ theo phong tục "Nam tả Nữ hữu"). Cả 2 mang viền vàng nét đứt hổ phách (`border-2 border-dashed border-amber-500 bg-amber-50/85`), cạnh hôn phối nối ngang phẳng phiu màu xanh ngọc bích `#059669`, và nút `Vị trí gốc ↗` lướt camera sang chi đối tác.
  2. *Bảo toàn số đinh:* Con cái chỉ vẽ dưới chân người cha ở Chi 1 theo huyết thống phụ hệ, nhánh người mẹ (Chi 2) không vẽ con cái khi xem Toàn họ, tránh tuyệt đối lỗi nhân đôi số lượng nhân đinh.
  3. *Cơ chế Ẩn/Hiện linh hoạt (`showInternalHusbands`):* Bổ sung tham số layout và công tắc switch trên Toolbar `[ 🔗 Hiển thị Rể nội tộc ]`: khi bật (mặc định), Ghost Node chồng xuất hiện bên trái vợ, thẻ vợ hiển thị footer chi nhánh gọn gàng; khi tắt, Ghost Node chồng được ẩn đi, thẻ vợ tự động kích hoạt footer điều hướng `🔗 Chồng: [Tên Chồng] [Xem gia đình ↗]` thay thế.
- **Flat Seamless Footer & Triệt Tiêu Lỗi Tràn Viền (Overflow Containment Guard):**
  1. *Triệt tiêu lỗi lồng hộp box-in-box:* Tuyệt đối không nhồi nhét một khối hộp nét đứt màu vàng bên trong thẻ thành viên (vi phạm nguyên tắc Anti Box-in-Box và dễ gây tràn viền ngoài khi text nút quá dài).
  2. *Thiết kế phẳng liền mạch (Seamless Hairline):* Chuyển footer thành 1 dòng đơn phẳng phiu, ngăn cách bởi đường hairline siêu mảnh `border-t border-slate-100 dark:border-slate-800/80 pt-1 mt-0.5`; text nút chuyển hướng rút gọn thành `Chi 1 ↗` dạng pill badge nhỏ gọn `whitespace-nowrap`, không bao giờ bị rớt dòng.
  3. *Bảo vệ chống tràn tuyệt đối:* Thẻ cha `MemberNode` trang bị `overflow-hidden`, đảm bảo $100\%$ không pixel nào có thể thò ra ngoài viền bo tròn `rounded-xl` của thẻ.
- **Loại Bỏ Chú Thích Phối Ngẫu Ngoại Tộc Thừa Thãi (Redundant In-Law Note Removal):**
  1. *Triệt tiêu chú thích lặp:* Khi người phối ngẫu (chồng/vợ ngoại tộc) đã được vẽ thành một node riêng biệt đứng kề bên trên canvas, thẻ người trong họ tuyệt đối không hiển thị dòng chú thích `Chồng: [Tên]`.
  2. *Xóa bỏ nhãn phân biệt:* Bỏ hoàn toàn nhãn `[Ngoại tộc]` vì người mang họ khác trong gia phả tự thân đã được nhận biết qua họ tên, tránh cảm giác phân biệt và chật chội. Thẻ người phụ nữ luôn hiển thị chân thẻ chuẩn mực gia tộc (`Chi nhánh • Số người con`).
- **Anonymous Ancestor Pattern & Cấu Trúc Khuyết Thế Hệ (Missing Generation Guard):** Khi gia phả bị thất truyền tư liệu một thế hệ (như Đời 2 họ Phạm Văn), tuyệt đối không nối tắt (làm sai lệch khoảng cách thế hệ và thuật toán LCA/Kinship) và không ép dồn thế hệ (làm tụt các đời sau vi phạm tôn ti tông tộc). Giải pháp chuẩn mực là tạo Node Khuyết danh (`is_anonymous: true`) có tên linh hoạt (`(Khuyết danh Đời 2)`, `(Khuyết danh 1 - Chi Trưởng)`), hiển thị viền nét đứt màu vàng hổ phách (`border-dashed border-amber-400`), giữ vững cấu trúc phân tầng $220\text{px}$ và bảo toàn 100% thứ bậc thế hệ.
- **Large Clan Stress Testing & Virtual Viewport Culling (`onlyRenderVisibleElements`):** Khi hiển thị gia tộc lớn lên đến $1.500$ nhân khẩu, việc kích hoạt `onlyRenderVisibleElements={true}` và `minZoom={0.05}` trong React Flow là điều kiện sống còn để trình duyệt không bị quá tải DOM node và giữ vững 60 FPS khi pan/zoom. Thuật toán phân cấp `calculateTreeLayout` xử lý $1.787$ nodes chỉ trong $9.78\text{ms}$ (vượt xa cam kết SLA $< 100\text{ms}$).
- **Slide-over Drawer vs Modal Dialog trong Phả Đồ Tương Tác:** Tránh dùng Modal popup ở giữa màn hình vì che khuất bối cảnh phả đồ. Slide-over Drawer từ cạnh phải màn hình (rộng 440px) giữ nguyên tầm nhìn trên canvas, kết hợp tính năng Quick Navigation lia camera mượt mà (`setCenter`) tới node của người thân khi được click trong danh sách Thân Tộc 1 Đời.
- **Mô hình Dàn Trang Đa Thê Một Phía & Phân Cụm Đàn Con (Left-to-Right Polygamy Topology & Sub-bus Separation):**
  1. *Nguyên tắc sắp xếp vợ chồng một phía:* Khi người đàn ông có nhiều vợ ($\ge 2$ bà), tuyệt đối không xếp chồng ở giữa các vợ (vi phạm quy ước Nam tả Nữ hữu và bế tắc khi có 3 bà vợ trở lên). Mô hình chuẩn mực là dàn hàng ngang sang phải: `[Chồng] ══ [Vợ Cả] ══ [Vợ Hai] ══ ...` với thứ tự `marriage_order` tăng dần.
  2. *Tách rời các cụm con song song (0 Crossing Lines):* Phân đàn con thành $K$ cụm nhỏ theo trật tự từ trái sang phải: Cụm con riêng khuyết mẹ (`mother_id: null` hạ từ chân người cha `children-single`) $\rightarrow$ Cụm con Vợ Cả (`children-spouse-0`) $\rightarrow$ Cụm con Vợ Hai (`children-spouse-1`). Việc cố định trật tự $X_{\text{con riêng}} < X_{\text{con bà cả}} < X_{\text{con bà hai}}$ đảm bảo các đường bus cắm thẳng $90^\circ$ xuống đỉnh các con mà tuyệt đối không bao giờ cắt chéo nhau.
  3. *Minh bạch hóa nhận diện dòng mẹ:* Trên thẻ con cái hiển thị Badge tím phân định mẹ (`[Con bà cả]`, `[Con bà hai]`, `[Chưa rõ mẹ]`), và trong Drawer thân tộc, danh sách Hậu Duệ của người cha tự động phân khối trang nhã theo từng bà mẹ để người xem nắm bắt trọn vẹn gia cảnh tiền nhân.
- **Multi-level Altitude Corridor & Phân tầng Cao độ Bus Y chống Bus Collision (Lồng đường đa thê & Con riêng của vợ):**
  1. *Căn nguyên Bus Collision (Lồng đường):* Khi nhiều cụm con (con riêng của cha, con các bà vợ) cùng chia sẻ một cao độ thanh ngang cố định (trung điểm $midY \approx 143\text{px}$), nếu một cụm con bị dàn trang dạt sang trái dưới chân người cha, đường ngang của cụm này sẽ cắt xuyên qua đường dọc của cụm khác tạo thành giao cắt chữ thập (lồng đường).
  2. *Giải pháp Phân tầng Cao độ Bus Y (Multi-level Altitude Corridor):* Trong `genealogy-layout.ts`, gán `busY` cho từng cụm theo công thức $Y_{bus}(\text{clusterIdx}) = Y_{\text{node}} + \text{NODE\_HEIGHT} + 25 + \text{clusterIdx} \times 20\text{px}$, và `FamilyBusEdge` đọc `data.busY`. Các tầng bus ngang chạy ở các cao độ khác nhau (chênh lệch $20\text{px}$), triệt tiêu $100\%$ hiện tượng lồng đè chập nét.
  3. *Điểm xuất phát nhánh con Vợ Hai / Vợ Ba:* Handle `children-spouse-1` neo tại $X = 540\text{px}$ (tâm đáy Vợ Hai) thay vì $X = 430\text{px}$ (khe giữa 2 bà vợ). Loại bỏ triệt để cảm giác thị giác kỳ quặc "con của 2 bà vợ".
  4. *Con riêng của người mẹ mang vào gia đình (`father_id != primary`):* Nhánh con hạ thẳng từ chân người mẹ (`source: sp.id`, `children-single`), sử dụng đường nét đứt tím phân biệt (`#a855f7`, `4 4`), hoàn toàn độc lập với thanh Bus của người cha.
- **Định nghĩa Thành viên Chưa Nối (Unlinked Member Detection & Dâu/Rể Exclusion):** Khi lọc danh sách thành viên chưa nối phả (`UnlinkedMembersDrawer`), tuyệt đối không dùng điều kiện ngây thơ `father_id IS NULL AND is_root = false`. Điều này sẽ bắt nhầm toàn bộ Dâu/Rể ngoại tộc vào khay mồ côi. Thuật toán đúng (`getUnlinkedMembers`) phải kết hợp đồ thị quan hệ: người nào dù khuyết cha mẹ ruột nhưng đã kết hôn với một thành viên đã nối trong cây (hoặc lan truyền quan hệ hôn nhân) thì được coi là đã nối phả hợp lệ.
- **Xóa Thành Viên An Toàn & Ngăn Ngừa Gãy Cành Phân Cấp (Safe Delete RESTRICT vs CASCADE):** Trong cây gia phả, tuyệt đối không dùng `ON DELETE CASCADE` hay `SET NULL` tự do cho cha mẹ, vì sẽ làm biến mất hoặc gãy vỡ hàng loạt nhánh con cháu thành các mảnh mồ côi không thể kiểm soát. Nguyên tắc an toàn: API chỉ cho phép xóa Node Lá (thành viên chưa từng có con cái trong CSDL); nếu thành viên đã có con cái, bắt buộc trả về mã lỗi RESTRICT yêu cầu điều chuyển hoặc xóa các con trước.
- **Topological Sorting Khi Nạp Excel Hàng Loạt (Bulk Excel Import Dependency Resolution):** Khi nhập dữ liệu gia phả từ Excel hàng loạt (`parseExcelFamilyTree`), các dòng dữ liệu thường bị xáo trộn thứ tự thế hệ (con nằm trước cha mẹ). Thuật toán Topological Sort dựa trên đồ thị có hướng (cha $\rightarrow$ con) kết hợp `generation_level` là bắt buộc để sắp xếp các bản ghi nạp vào CSDL theo thứ tự tiền nhân trước, hậu duệ sau. Điều này đảm bảo toàn vẹn khóa ngoại `father_id` / `mother_id` và tính toán chuẩn xác thứ tự sinh `birth_order` ngay khi import.
- **Tương thích TypeScript App Router Stream & Downlevel Iteration:** Khi xuất file template Excel nhị phân (`xlsx`) từ Next.js App Router, dùng `new Response(Buffer.from(buffer), { headers: ... })` để giải quyết triệt để xung đột kiểu `BodyInit`. Đồng thời, tránh cú pháp `for (const [k, v] of map.entries())` nếu compiler chưa bật `downlevelIteration`; thay vào đó sử dụng `map.forEach((val, key) => ...)` để build sạch sẽ và tương thích 100% môi trường.
- **Bẫy Kế Thừa Màu Chữ Dark Mode Toàn Cục (CSS Contrast Inheritance Trap):** Khi thiết lập Tailwind `darkMode: ['class']`, nếu `globals.css` định nghĩa `--foreground-rgb: 248, 250, 252` (chữ trắng) qua `@media (prefers-color-scheme: dark)` cho `body`, nhưng thẻ `<html>` chưa gắn class `.dark`, toàn bộ modal nền trắng (`bg-white`) sẽ làm các thẻ `<span/>` không có class màu tường minh biến thành chữ trắng trên nền trắng (contrast 1:1, vô hình với mắt người). Bài học: Các control quan trọng (như chọn Giới tính) bắt buộc dùng Pill Buttons có class màu chữ và viền tường minh cho cả 2 chế độ (`bg-blue-50 text-blue-700 dark:text-blue-300`).
- **Kiến Trúc Form Gia Phả 1 Màn Hình Thống Nhất (Single-Screen Adaptive Form with Dynamic Disclosure):** Chia tab trong form gia phả gây giấu thông tin (phải click 3 tab, khó phát hiện trường lỗi). Mô hình tối ưu là Form 1 màn hình cuộn mượt với **Khung 3 Tầng** (Fixed Header, Scrollable Body với thanh cuộn siêu mảnh 5px, Sticky Footer - nút Lưu luôn cố định ở đáy) kết hợp **Dynamic Disclosure**: Mặc định chọn `Còn sống` ẩn 100% các trường ngày mất Âm lịch (form chỉ ~450px, không cần cuộn cho 80% trường hợp trẻ em/dâu rể); khi chọn `Đã mất †`, cụm âm lịch (ngày, tháng, Can Chi, mộ phần) mở ra tự nhiên.
- **Tạo Phối Ngẫu Ngoại Tộc Tại Chỗ (Inline Spouse Creation):** Trong thực tế 90% Dâu/Rể là người ngoài họ (chưa từng có trong CSDL). Nếu form chỉ có dropdown chọn người sẵn có, luồng nhập liệu sẽ bị bế tắc. Biểu mẫu chuẩn mực phải cung cấp ô nhập trực tiếp Tên Vợ/Chồng ngoài họ (tự động suy luận giới tính ngược chiều và tạo cả bản ghi thành viên lẫn `spouse_relations`), chỉ dùng dropdown cho trường hợp hôn nhân nội tộc.
- **Flat Seamless Modern Heritage & Triệt Tiêu Box-in-Box Trong Form Phức Tạp:** Khi thiết kế các form nhập liệu nhiều trường (như hồ sơ gia phả), việc lồng các khối hộp (`rounded-xl bg-slate-50 border`) lồng vào nhau tạo cảm giác bí bách, ngột ngạt và rẻ tiền. Giải pháp chuẩn mực là phong cách phẳng Flat Seamless: nền phẳng trắng sứ đồng nhất, phân tách các phân khu bằng đường hairline siêu mảnh 1px (`border-t border-slate-100 dark:border-slate-800 pt-6 mt-6`), tiêu đề phân khu thanh thoát với icon sắc nét. Điều này giúp giải phóng không gian, tăng diện tích thở và tạo ấn tượng cao cấp.
- **Bố Cục Chống Rớt Dòng & Kiểm Soát Quyền Gốc Tự Động:** (1) Trong các ô nhập thứ tự sinh có nhãn giải nghĩa dài, tuyệt đối không ép vào `grid-cols-4` hẹp (~110px) làm chữ bị gãy thành 2 hàng lem nhem; phải tách thành 2 hàng: hàng 1 dành riêng cho ô số thứ tự và nhãn giải nghĩa thoáng đãng, hàng 2 dành cho các tag đặc điểm (con trưởng, con nuôi). (2) Cờ Cụ Tổ (`is_root`) là thuộc tính khởi nguyên duy nhất của cây, tuyệt đối không đưa checkbox này vào form nhập liệu con cháu thông thường để triệt tiêu vĩnh viễn rủi ro logic tạo ra nhiều cụ tổ trong họ tộc.
- **Đồng Nhất Hệ Thống Component Toàn Form (Design System Unification):** Tránh hiện tượng phân mảnh UI trong cùng một form (mục trên dùng Pill Button, mục dưới dùng radio button tròn cổ điển; mục này dùng radio phân tán, mục kia dùng button). Mọi lựa chọn giới tính (thành viên chính, con cái) phải dùng chung một chuẩn Pill Button tương phản cao; mọi cụm lựa chọn chế độ (phối ngẫu) phải dùng Segmented Control 3 phân đoạn đồng bộ.
- **React Flow Viewport Culling (`onlyRenderVisibleElements`) & Node Dimensions (`width`/`height`):** Thuộc tính `onlyRenderVisibleElements={true}` của React Flow phụ thuộc hoàn toàn vào bounding box của các nodes để quyết định render hay ẩn. Nếu các node trả về từ engine layout không khai báo tường minh `width` và `height` trên từng node object (chỉ định nghĩa qua CSS), React Flow coi bounding box là $0 \times 0$ tại vị trí chưa mount, dẫn đến việc culling chặn toàn bộ các node không cho mount vào DOM (gây hiện tượng màn hình đen rỗng). Giải pháp cốt lõi: (1) Bắt buộc khai báo tường minh `width: NODE_WIDTH (200)`, `height: NODE_HEIGHT (96)` trực tiếp trên 100% object node (`memberNode` & `ghostNode`); (2) Chỉ kích hoạt `onlyRenderVisibleElements` đối với các dataset khổng lồ ($\ge 1.500$ nodes), tắt cho các dataset nhỏ và vừa để render tức thì 0ms.
- **Supabase Service Role Bypass RLS Trên Server Mutation Routes:** Khi thiết kế các API Route thực hiện Mutation (INSERT/UPDATE/DELETE) trên Next.js App Router, việc dùng client ẩn danh (`createClient()` với Anon Key) sẽ bị rào cản Row Level Security (RLS) của Supabase từ chối âm thầm nếu bảng không có chính sách công khai cho phép ghi. Bắt buộc tách biệt: Client-side đọc dữ liệu qua Anon Key; Server API routes thực hiện ghi/xóa/sửa bằng `createAdminClient()` sử dụng `SUPABASE_SERVICE_ROLE_KEY` (`auth: { autoRefreshToken: false, persistSession: false }`), đảm bảo dữ liệu được ghi bền vững vào CSDL và lưu tồn qua mọi lần tải lại trang (F5).
- **Thẩm Mỹ Refined Modern Heritage - Triệt Tiêu Bo Tròn Quá Mức (Anti-Bubbly Aesthetics):** Trong thiết kế giao diện di sản văn hóa số, việc lạm dụng góc bo quá tròn (`rounded-2xl`, `rounded-full`) tạo cảm giác bồng bềnh, đồ chơi và đại trà (bubbly/toyish UI). Chuẩn mực cao cấp hiện đại là các đường nét hình học sắc sảo: góc bo chuẩn `rounded-lg` (8px) cho modal/khung chứa và `rounded-md` (6px) cho control/input; tiêu đề các phân mục sử dụng Editorial Micro-Headers thanh mảnh, chữ in hoa giãn chữ (`text-[11px] font-bold tracking-widest uppercase text-slate-400 dark:text-slate-500`) kết hợp đường kẻ phân cách hairline siêu mỏng 1px (`border-t border-slate-100 dark:border-slate-800`), loại bỏ hoàn toàn các icon màu mè sặc sỡ để tôn vinh tính trang trọng, tinh tế của hồ sơ gia phả.
- **Cách Ly Thư Mục Build Kiểm Chứng (`NEXT_DIST_DIR` & `distDir` Isolation):** Khi tiến hành chạy kiểm chứng Tầng 1 (`npm run build`) trong khi dev server (`npm run dev`) đang chạy nền trên máy người dùng, Next.js mặc định ghi đè toàn bộ thư mục `.next/` bằng mã nguồn production. Điều này xóa sạch các development chunks và khiến dev server trả về HTTP 404 cho 100% file JavaScript của client (làm tê liệt hoàn toàn React Hydration). Giải pháp triệt để: Định nghĩa `distDir: process.env.NEXT_DIST_DIR || '.next'` trong `next.config.mjs` và thực thi verify build với `NEXT_DIST_DIR=.next-build npm run build`. Dev server dùng độc quyền `.next/`, tiến trình build dùng riêng `.next-build/`, triệt tiêu 100% nguy cơ xung đột cache và 404 chunks.
- **Bộ 3 Cơ Chế "Bất Khả Lạc" Cho Camera React Flow (`defaultViewport` + `useNodesInitialized`):** Trong các đồ thị có kích thước lớn ($> 3.000\text{px}$) với gốc tọa độ lệch tâm (như Cụ Khởi $X = 1.845$), việc dựa dẫm vào prop `fitView` duy nhất lúc mount rất dễ bị trượt do container DOM chưa ổn định kích thước, khiến camera kẹt tại khoảng tối $(0, 0)$. Giải pháp chuẩn mực: (1) Cung cấp `defaultViewport={{ x: -600, y: 40, zoom: 0.55 }}` để ngay từ frame 0ms đầu tiên camera đã nhắm thẳng vào cụm node gốc; (2) Dùng hook chính thức `useNodesInitialized()` của `@xyflow/react` để tự động gọi `fitView({ padding: 0.25, duration: 400 })` ngay khi 100% thẻ được DOM đo xong kích thước; (3) Lắng nghe `currentDataset` và `focusRootId` để tự động kích hoạt `fitView` khi đổi bộ dữ liệu.
- **Đồng Bộ Hóa Theme Toàn Diện & Chống Sụp Đổ Viewport Flexbox (Theme & Viewport Resilience):**
  1. *Bẫy Xung Đột `@media (prefers-color-scheme)` vs Tailwind `darkMode: ['class']`:* Khi Tailwind cấu hình `darkMode: ['class']`, nếu `globals.css` lại dùng `@media (prefers-color-scheme: dark)` tự ý ép `body { background: #090e1a }` khi máy người dùng bật Dark Mode trong khi thẻ `<html>` chưa có class `.dark`, toàn bộ Tailwind component sẽ render ở Light Mode (nền trắng). Hậu quả là sinh ra giao diện "Frankenstein" nửa sáng nửa tối (Card trắng bóc lơ lửng trên nền đen kịt). Giải pháp: Xóa bỏ hoàn toàn `@media` ép body trong `globals.css`, chỉ dùng selector `.dark` đồng bộ với Tailwind; bổ sung inline theme script chống FOUC trong `layout.tsx` và nút bấm `ThemeToggle` trên Navbar để người dùng làm chủ giao diện.
  2. *Sụp Đổ Chiều Cao Flexbox & Viewport Canvas React Flow:* Trong CSS Flexbox, nếu chuỗi cha-con (`body` $\rightarrow$ `<main>` $\rightarrow$ `TreePage` $\rightarrow$ `FamilyTreeCanvas`) thiếu resolved height tường minh hoặc thiếu `min-h-0 relative`, các container con dùng `h-full` sẽ bị sụp đổ chiều cao về 0px. Kết hợp với việc `layout.tsx` kẹp cứng thẻ `<footer>` 60px ở đáy, container canvas bị co hẹp hoặc tràn ra ngoài. Khi container có `height: 0`, React Flow không thể khởi tạo viewport để render nodes, và người xem sẽ nhìn xuyên thấu container xuống nền đen của `body`. Giải pháp: (1) Thêm `min-h-0 relative` trên `<main>`; (2) Đặt `h-[calc(100vh-4rem)] min-h-[500px]` trên `TreePage` và `FamilyTreeCanvas`; (3) Ẩn `AppFooter` trên route `/tree` để canvas chiếm trọn vẹn 100% viewport.
- **Chuyên Biệt Hóa Form Theo Ngữ Cảnh (Contextual Form Specialization - Thêm Phối ngẫu vs Thêm Con cái):** Thay vì dùng một form "vạn năng" cồng kềnh với hàng tá trường không liên quan (hỏi bố mẹ ruột của dâu ngoại tộc, hoặc hỏi phối ngẫu của con mới sinh), việc khóa cứng đối tác liên quan (`targetPartner` [🔒]) và ẩn hoàn toàn các khối không cần thiết (khi là Dâu ngoại tộc thì ẩn Khối 2 Thân tộc & Khối 4 Con cái; khi Thêm con thì khóa cứng Bố/Mẹ và ẩn Khối 3 Phối ngẫu) giúp form giảm từ 850px xuống chỉ còn ~350px, không cần cuộn, thao tác nhanh và triệt tiêu 100% khả năng người dùng nhập sai/chọn nhầm chồng hoặc bố mẹ khác.
- **Dọn Trùng Thứ Tự Sinh & Chuyển Giao Con Trưởng Tự Động (Frictionless Birth Order & Senior Demotion):** Khi người dùng nhập trùng thứ tự sinh `birth_order` giữa các anh chị em ruột, thay vì chặn cứng (hard error) gây phiền hà cho người nhập liệu gia phả, hệ thống tự động xóa bỏ `birth_order = null` của người con cũ để nhường số cho người mới. Đồng thời, khi gán Con Trưởng (`is_senior`), bật modal xác nhận chuyển giao và tự động hạ cờ `is_senior = false` của con trưởng cũ, bảo toàn nguyên tắc bất biến: tại mọi thời điểm trong 1 gia đình chỉ có tối đa 1 Con Trưởng.
- **Kiểm Thực Nghịch Lý Sinh Học 2 Chiều (Bi-directional Biological Age Paradox):** Ràng buộc năm sinh con không thể $\le$ năm sinh bố mẹ (`validateParentChildAge`) phải được kiểm tra 2 chiều: (1) Khi tạo/sửa con cái so với năm sinh bố mẹ; (2) Khi sửa năm sinh của bố mẹ so với năm sinh của toàn bộ các con hiện có. Lỗi phải ném mã HTTP 400 và chỉ rõ đích danh tên cùng năm sinh người liên quan, kèm cảnh báo mềm (soft warning) khi khoảng cách tuổi quá gần ($< 15$ tuổi).
- **Chuẩn Hóa Danh Xưng Tập Trung Qua `KINSHIP_TERMS` (Single Source of Truth Vocabulary):** Toàn bộ danh xưng xưng hô gia đình thuần Việt (`Bố mẹ`, `Anh em ruột`, `Con cái`, `Vợ cả`, `Vợ hai`, `Dâu ngoài họ`, `Dâu nội tộc`, ...) phải được định nghĩa tập trung trong 1 file hằng số duy nhất (`src/constants/kinship-terms.ts`), triệt tiêu hoàn toàn việc phân mảnh xưng hô (chỗ gọi "Phụ mẫu", chỗ gọi "Bố mẹ", chỗ gọi "Huynh đệ", chỗ gọi "Anh em").
- **Tính Toán Song Song Tuổi Dương & Tuổi Mụ Kèm Tooltip Công Thức (Dual Age Calculation with Tooltip):** Người dùng gia phả thường quen tính cả tuổi dương và tuổi mụ truyền thống. Việc hiển thị song song `SN 1998 (28 tuổi · 29 mụ) ℹ️` kèm tooltip giải thích chi tiết công thức (`Tuổi dương = Năm hiện tại - Năm sinh; Tuổi mụ = Tuổi dương + 1`) giúp minh bạch hóa và tránh hiểu nhầm cho mọi thế hệ người xem.
- **Hôn nhân nội tộc trong Form và Nguyên tắc Thực thể Duy nhất (Single Record Policy):** Khi kết hôn nội tộc (`spouseOrigin === 'internal'`), cả 2 thành viên đều đã có hồ sơ trong CSDL gia phả. Biểu mẫu submit BẮT BUỘC chỉ gọi `POST /api/spouse-relations` để liên kết hai ID đã có; tuyệt đối CẤM gọi `POST /api/members` vì sẽ tạo bản ghi nhân bản làm phá vỡ tính toàn vẹn và vi phạm nguyên tắc `[R-SPEC]`.
- **Safe Delete RESTRICT & Trải nghiệm Người dùng trên Drawer:** Thao tác xóa thành viên từ Drawer phải được kiểm soát chặt chẽ bởi `canDeleteMember`. Nếu thành viên đang có con cái, nút `[🗑️ Xóa hồ sơ]` bị vô hiệu hóa (disabled) kèm tooltip giải thích lý do; chỉ khi là Node Lá (0 con) nút mới kích hoạt màu đỏ và hiển thị hộp thoại xác nhận trước khi gửi lệnh `DELETE /api/members/[id]`.
- **Fail-Fast Error Integrity trong Bulk Import API:** Khi thực hiện ghi dữ liệu hàng loạt vào CSDL qua `createAdminClient()`, tuyệt đối cấm `try/catch` nuốt lỗi DB rồi trả về `{ success: true }` ảo. Mọi lỗi insert/delete từ Supabase phải ném lỗi ngay (fail-fast) và trả về HTTP 500 kèm chi tiết lỗi để bảo toàn tính liêm chính của dữ liệu.
- **Đồng bộ chuẩn hình học Crisp Architectural Geometry trên toàn hệ thống:** Thay thế triệt để các góc bo quá tròn (`rounded-2xl`, `rounded-3xl`) trên các trang quản trị bằng `rounded-lg` (8px) và `rounded-md` (6px) để đồng nhất với ngôn ngữ thiết kế Refined Modern Heritage, giữ cho giao diện luôn sắc sảo, đĩnh đạc và trang trọng.
- **Ingestion Pipeline & Lineage Integrity Principle (Bảo toàn Tính nguyên bản Quan hệ Cha - Con khi chuyển đổi Phả hệ Cổ truyền):**
  1. *Chống ảo giác phả hệ (Anti-Hallucination Guard):* Khi chuyển đổi dữ liệu từ các văn bản gia phả cổ truyền dạng bảng phẳng thế hệ (Word/Markdown), tuyệt đối CẤM AI tự ý suy đoán hoặc gán quan hệ Cha - Con (`STT Bố`) cho các thế hệ phân nhánh (từ Đời 5 $\rightarrow$ Đời 14). Cột `STT Bố` và `STT Mẹ` bắt buộc để trống (`null`) để người thật đối chiếu điền theo sổ phả gốc.
  2. *Tự động hóa 100% Cụm Hôn Phối & Ngày Giỗ Âm Lịch:* Tự động liên kết cặp vợ chồng hai chiều từ cấu trúc cụm hạt nhân (`Vợ cả:`, `Vợ hai:`, `Chồng:`), dùng Regex bóc tách chuẩn xác Ngày và Tháng mất Âm lịch đưa vào 2 cột riêng biệt, và liên kết truyền đơn trực hệ Đời 1 $\rightarrow$ Đời 4 đã được xác thực (ngăn ngừa triệt để lỗi tự trỏ self-loop).
  3. *Nhận diện Node Lá (Leaf Node Recognition):* Bảo toàn các ghi chú tiền nhân như `"Không con chết sớm"`, `"Chết không con"`, `"Không vợ con"`, `"Đi tu – chết sớm"` để người nhập liệu nhận diện ngay các Node Lá (0 con), không mất công tìm kiếm con cháu ở các đời sau.


